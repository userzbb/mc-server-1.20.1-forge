# 🧱 Minecraft 服务器操作手册

> 最后更新：2026-07-14

---

## 目录

1. [服务器信息](#1-服务器信息)
2. [日常操作](#2-日常操作)
3. [MCSManager 管理面板](#3-mcsmanager-管理面板)
4. [防火墙](#4-防火墙)
5. [DDNS-GO 动态域名](#5-ddns-go-动态域名)
6. [Mod 管理](#6-mod-管理)
7. [常见问题](#7-常见问题)

---

## 1. 服务器信息

| 项目 | 值 |
|------|-----|
| 内网 IP | `192.168.1.11` |
| 公网 IPv4 | `117.151.60.221`（路由器端口转发） |
| 公网 IPv6 | `2409:8a4c:1210:c6c0:f824:6446:a7e3:cbe4` |
| 域名 | `catzizimiku.top` |
| 服务器目录 | `~/minecraft-server` |
| sudo 密码 | `1970` |

### 端口一览

| 端口 | 协议 | 用途 | 访问方式 |
|------|------|------|---------|
| `25565` | TCP | Minecraft 游戏 | ✅ IPv6 直连（推荐），IPv4 需路由器转发 |
| `23333` | TCP | MCSManager 面板 | ✅ IPv6 直连 |
| `24444` | TCP | MCSManager 守护进程 | IPv6 直连（浏览器 WebSocket） |
| `25575` | TCP | RCON 远程管理 | 内网使用 |
| `24454` | UDP | 语音聊天 | IPv6 直连 |
| `9876` | TCP | DDNS-GO 管理页面 | host 网络直通 |

> 所有服务都通过域名 `catzizimiku.top` 走 **IPv6** 访问，不需要端口转发。

---

## 2. 日常操作

### 启动 / 停止 / 重启 所有服务

```bash
cd ~/minecraft-server

# 启动（后台）
docker compose up -d

# 停止
docker compose stop

# 重启
docker compose restart

# 查看运行状态
docker compose ps

# 完全停止并删除容器
docker compose down
```

### 查看日志

```bash
# Minecraft 服务器日志
docker compose logs -f mc-forge

# MCSManager 日志
docker compose logs -f mcsm-web
docker compose logs -f mcsm-daemon

# DDNS-GO 日志
docker compose logs -f ddns-go
```

### 更新 Mod

1. 下载 `.jar` 文件放到 `~/minecraft-server/mods/` 目录
2. 重启服务器：`docker compose restart mc-forge`

---

## 3. MCSManager 管理面板

### 访问

```
地址：http://catzizimiku.top:23333
账号：zizimiku
密码：Dyf0130.com
```

> ⚠️ 用 **HTTP** 不是 HTTPS！浏览器默认 HTTPS 会连不上。

### 日常功能

**查看服务器状态：** 登录后首页就能看到 CPU、内存占用

**管理 MC 服务器：** 左侧「终端」→ 点击「主服务器」节点 → 点实例

**上传/下载文件：** 点进实例 → 「文件管理」，可以直接拖拽上传 mod、存档等

**控制台操作：** 点进实例 → 「控制台」，可以输入命令如 `/op 玩家名`

### ⚙️ 网络架构说明

MCSManager 的浏览器与守护进程通过 **WebSocket** 连接。
- **浏览器 → 守护进程**：通过域名 `catzizimiku.top:24444` 走 IPv6
- **后端（Web 容器）→ 守护进程**：通过 `extra_hosts` 指向 `host-gateway`，路由到主机端口

如果守护进程离线，检查以下步骤：

```bash
# 检查是否运行
docker compose ps mcsm-daemon

# 从 Web 容器测试连通性
docker exec mcsm-web node -e "const http = require('http'); http.get('http://catzizimiku.top:24444/', r => { r.resume(); console.log(r.statusCode) }).on('error', e => console.log(e.message))"
```

### 如果配置丢了，手动添加节点

1. 面板 → 左侧「节点」→「添加节点」
2. 填写：
   - 名称：`主服务器`
   - IP：`catzizimiku.top`（域名，不要写容器名或 127.0.0.1）
   - 端口：`24444`
   - 密钥：`3fa29ec75e5f4c66cbb29eed3de41b798e73b18f29061fe`
   - 点击确认

>   配置保存在 `mcsm/web/data/RemoteServiceConfig/<UUID>.json`，也可以直接改这个文件重启面板生效。

---

## 4. 防火墙

所有端口统一集成在 `mc-server` 这个 firewalld 服务里。

> ⚠️ 物理网卡 `enp2s0` 绑定的是 **FedoraServer** 区域（不是 FedoraWorkstation）。
> 两个区域都需要勾选 `mc-server` 服务，否则外网连不上。

### 在 GUI 里修改

终端运行：`! firewall-config`
在打开的窗口里：
- **服务 → mc-server**：可以编辑这个服务包含的端口
- **区域 → FedoraWorkstation**：勾选 `mc-server` 服务
- **区域 → FedoraServer**：勾选 `mc-server` 服务（同样重要！）

### 命令行查看

```bash
# 查看 mc-server 服务包含的端口
sudo firewall-cmd --info-service=mc-server

# 查看启用了哪些服务
sudo firewall-cmd --list-services

# 重载防火墙
sudo firewall-cmd --reload
```

### mc-server 服务包含的端口

| 端口 | 用途 |
|------|------|
| 9876/tcp, 9876/udp | DDNS-GO |
| 25565/tcp, 25565/udp | Minecraft 游戏 |
| 25575/tcp, 25575/udp | RCON |
| 24454/tcp, 24454/udp | 语音聊天 |
| 23333/tcp | MCSManager 面板 |
| 24444/tcp | MCSManager 守护进程 |

---

## 5. DDNS-GO 动态域名

### 访问

```
http://catzizimiku.top:9876
```

### 功能

把你的公网 IP 自动绑定到域名 `catzizimiku.top`，IP 变了域名自动更新。

支持：
- ✅ IPv4 + IPv6 双栈
- ✅ 阿里云 DNS

### 如果域名解析不对

```bash
# 检查 DNS 解析
host catzizimiku.top

# 检查当前公网 IP
curl -4 https://myip.ipip.net
curl -6 https://myip.ipip.net

# 重启 DDNS-GO
docker compose restart ddns-go

# 查看 DDNS-GO 日志
docker compose logs ddns-go
```

---

## 6. Mod 管理

### Mod 清单

完整列表见 `mod-list.md`。

### Mod 目录

```
~/minecraft-server/mods/    ← 放 jar 文件的地方（只读挂载给容器）
```

### ⚠️ 注意

**不要安装这些客户端 Mod 到服务器上**，会崩溃：

| Mod | 原因 |
|-----|------|
| `entity_texture_features` | Mixin 引用客户端类 → ❌ 崩溃 |
| `oculus` | 客户端光影 Mixin → ❌ 崩溃 |
| `sodiumextras` | 客户端渲染 Mixin → ❌ 崩溃 |
| `DGLabCraft` | 客户端 UI Mixin → ❌ 崩溃 |

其他客户端 mod（如 embeddium、JEI 等）虽然不会崩，但也没必要放服务器上。

---

## 7. 常见问题

### Q: 公网连不上怎么办？

**IPv4 方式：**
服务器在路由器后面（`192.168.1.11`），需要路由器做端口转发：
1. 浏览器打开 `http://192.168.1.1`
2. 找到「端口转发」或「虚拟服务器」
3. 把需要的端口转发到 `192.168.1.11`

**IPv6 方式（推荐）：**
如果你的网络有 IPv6，直接连接即可。域名 `catzizimiku.top` 已绑定了 IPv6。

### Q: Docker 拉取镜像太慢/超时？

配置了国内镜像加速 `docker.1ms.run`，如果还慢可以换一个：

```bash
sudo tee /etc/docker/daemon.json << 'EOF'
{
  "registry-mirrors": ["https://docker.1ms.run"]
}
EOF
sudo systemctl restart docker
```

其他可用镜像源：
- `https://docker.m.daocloud.io`
- `https://dockerproxy.com`

### Q: MCSManager 面板提示「无法连接到远程节点」？

如果你看到类似 `ws://xxx:24444` 连接失败的错误：

1. 先确认守护进程在线：`docker compose ps mcsm-daemon`
2. 确认节点配置里的 IP 是 `catzizimiku.top` 不是 `mcsm-daemon` 或 `127.0.0.1`
3. 从 Web 容器测试连通性：
   ```bash
   docker exec mcsm-web node -e "const http = require('http'); http.get('http://catzizimiku.top:24444/', r => { r.resume(); console.log(r.statusCode) }).on('error', e => console.log(e.message))"
   ```
4. 如果连不上，在 `docker-compose.yml` 的 `mcsm-web` 里检查 `extra_hosts` 配置是否正确：
   ```yaml
   extra_hosts:
     - "catzizimiku.top:host-gateway"
   ```
5. 然后重启：`docker compose restart mcsm-web`

> 浏览器 WebSocket 需要通过域名走 IPv6 直连守护进程，所以节点 IP 必须是域名，不能是 Docker 内部地址。

### Q: 服务起不来，端口被占用？

```bash
# 查谁占了端口
sudo ss -tlnp | grep <端口号>

# 最常见的冲突：mc-forge 和 MCSManager 创建的实例用了同个端口
# 解决方法：关掉一个
```

### Q: 想重置整个服务器？

```bash
cd ~/minecraft-server
docker compose down -v          # 停止并删掉所有容器和网络
rm -rf data/ mcsm/               # 清除数据
git checkout -- .                # 恢复配置文件到 git 版本
docker compose up -d             # 重新启动
```

---

## Git 管理说明

仓库在 `~/minecraft-server`，已初始化 git。

```bash
# 查看改了什么
git status

# 提交修改
git add -A
git commit -m "说明改了什么"

# 查看提交历史
git log --oneline
```

### 被 git 忽略的（不上传）

- `data/` — 世界存档、日志等运行时数据
- `ddns-go-data/` — DDNS 运行时数据
- `mcsm/` — MCSManager 数据
- `mods/*.jar` — Mod 二进制文件（用 `mod-list.md` 记录）
- `credentials.md` — 密码等敏感信息
- `*.bak` — 备份文件
- `.claude/` — Claude Code 配置

---

> **凭据汇总** 见同目录下的 `credentials.md`（已 gitignore，注意备份）
